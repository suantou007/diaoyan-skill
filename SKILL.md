---
name: diaoyan
version: 3.6.0
description: "基于产品演示录屏与定向网页校验做竞品调研，默认使用 Tavily 做网页核验、使用飞书文档做正式交付；若 Tavily 或飞书未配置，先询问用户是否安装/配置，而不是静默降级。适用于 competitor analysis、product video research、敏捷研究、竞品调研、竞品分析、feature evidence extraction，或用户提供 .mp4/.mov/.webm 录屏并希望获得结构化结论的场景。"
---

# 基于录屏的产品竞品调研

> 对外仓库名为 `diaoyan-skill`；skill 名为 `diaoyan`。旧的 `product-competitor-research` 语义只作为触发词兼容，不再作为内部名称。

这个 skill 用来把产品演示录屏整理成**证据型竞品研究**。默认先完成本地 evidence package，再**用 Tavily 做网页核验、发布飞书文档**。如果 Tavily 或飞书未配置，不要静默降级；先询问用户是否现在安装/配置。

## 何时使用

- 用户给出 `.mp4` / `.mov` / `.webm` 录屏，希望看懂产品做了什么
- 用户要竞品分析、敏捷研究、正式调研报告
- 用户需要带时间戳、截图、网页核验和开放问题的结论

## 默认工作流

1. intake：补齐视频路径、产品名、官网和重点问题
2. 浏览视频并提取稳定截图
3. 用 Tavily 核验官网 / 文档 / 定价 / 更新日志
4. 用 WebSearch 补 Tavily 漏掉的关键事实（尤其价格 / 套餐 / 限制 / 日期）
5. 产出本地 evidence package
6. 默认发布飞书；同步产出同结构的 `notes.html`

## 开始前只检查必要工具

- `ffmpeg` / `ffprobe`
- `python3` + Pillow（供 `scripts/prepare_llm_images.py` 使用）
- **Tavily CLI（`tvly`）**：默认网页核验必须使用；如果没装 / 未登录 / 不可用，先问用户要不要现在安装或配置
- **飞书 CLI（`lark-cli`）**：默认正式交付必须使用；用 `lark-cli auth status` 检查认证，发布时使用 `lark-cli docs ...`；需要文档能力时可调用 `lark-doc` skill。如果没装 / 未登录 / token 失效，先问用户要不要现在配置或刷新

## 五条硬规则

1. **证据分层**：始终分开写
   - 视频观察
   - 网页确认
   - 推断 / 开放问题

2. **正式报告必须读结构文件**：只要交付飞书或 `notes.html`，就必须覆盖 [references/reference_doc_structure.md](references/reference_doc_structure.md)。缺失项也要写成“未找到 / 未核验 / 视频未展示”，不能静默省略。

3. **正式竞品分析必须运行时检索用户 skill 库**：优先使用当前会话暴露的 skills 列表；如需发现懒加载工具，用 `tool_search`。优先寻找 `tavily-search`、图示 / 飞书类 skill；如果用户明确要深度纵横研究，再补 `hv-analysis`。在 `analysis_manifest.json` 记录 `skills_consulted`。

4. **需要流程 / 竞品关系 / 能力地图时必须补图**：发布飞书时先读 [references/diagram_workflow.md](references/diagram_workflow.md)。默认优先 `SVG → 飞书画板`；HTML 兜底插入 SVG / PNG。

5. **默认一定是 Tavily + 飞书**：不要把内置 WebSearch / `web.run` 或本地 HTML 当默认路径。`tvly` 不可用时，先问用户“要不要现在安装/配置 Tavily？”；飞书不可用时，先问用户“要不要现在配置/刷新飞书 CLI？”。只有用户明确同意降级，才能不用 Tavily 或不发飞书。

## 只保留 4 份参考文件

1. [references/workflow.md](references/workflow.md) —— 执行手册：视频分析、网页核验、本地输出、飞书发布、QA
2. [references/reference_doc_structure.md](references/reference_doc_structure.md) —— 正式报告必须覆盖的信息
3. [references/diagram_workflow.md](references/diagram_workflow.md) —— 什么时候补图、图放哪里、怎么走飞书画板
4. [references/checklist.md](references/checklist.md) —— 最终 QA 与发布前检查

额外只用：
- `scripts/prepare_llm_images.py`：安全图压缩，避免 413
- `assets/notes_template.html`：`notes.html` 模板

## 执行顺序

### 1. Intake

确认：
- 录屏路径
- 产品名 / 官网（已知则直接用）
- 用户最关心的问题
- 当前环境的 `tvly` 是否可用；如果不行，先问用户要不要安装/配置 Tavily
- 当前环境是否能正常发布飞书；如果不行，先问用户要不要配置/刷新飞书 CLI

### 2. 视频证据

按 `references/workflow.md` 的视频流程做：
- 先全片 survey，再下判断
- 先生成 `llm_images/`，再给模型 / 子 agent 看图
- 不发原图、不贴 base64、不用 `detail: "original"`

### 3. 网页核验

至少核验：
- 官方首页
- 官方 docs / help
- pricing / plans
- changelog / release notes / blog（视频明显展示新功能时）

默认必须使用 **Tavily CLI（`tvly`）** 做这些查询。不要把内置 WebSearch / `web.run` 当默认方案。但如果 Tavily 没抓到关键事实，尤其是**价格、套餐额度、关键限制、具体日期**，就必须继续补：

- 先补 **内置 WebSearch / `web.run`**
- 页面强动态或需要登录态时，再补 `web-access`

也就是说：**Tavily 是默认入口，不是唯一来源。关键事实没抓到时，不能停在“未抓到”。**

### 4. 本地交付资产

默认保留：
- `frames_10s/`
- `contact_sheets/`
- `selected_screenshots/`
- `llm_images/`
- `notes.html`
- `analysis_manifest.json`

### 5. 正式交付

- 默认必须发布飞书
- 飞书发布前先看结构，再看图示，再写正文
- 如果飞书 CLI / 认证不可用，先问用户要不要现在配置或刷新；只有用户明确同意降级，才退回 `notes.html`

## 报告至少要覆盖

除非用户明确只要快速草稿，否则至少覆盖：

1. 一句话总结：`{一句话定位} · {一句话亮点} · {一句话现状描述}`
2. 基本信息：产品名、公司 / 团队、上线时间、平台、产品定位、目标用户、关键数据、官网 / 下载
3. 产品简介：150–250 字自然段
4. 核心功能体验：按视频顺序写，小标题 → 简述 → 体验截图 → 截图描述 → 一句点评
5. 亮点与不足：聚焦本产品自身体验，总数控制在 6 条以内
6. 竞品对比：本产品 + 同赛道 2–3 个产品
7. 结论与跟踪建议：核心判断 + 值得持续跟踪的变量
8. 一致性检查：产品定位 vs 核心判断；亮点 / 不足 vs 核心功能体验
9. 附录：体验视频、外部检索、其他参考

## 飞书写法风格（按 Wiki 模板收敛）

默认复刻这种写法特征：

- **章节编号清楚**：按“一句话总结 / 基本信息 / 产品简介 / 核心功能体验 / 亮点与不足 / 竞品对比 / 结论与跟踪建议 / 一致性检查 / 附录”组织
- **基本信息先行**：先给产品定位、目标用户、关键数据等读者建立判断所需的锚点
- **HTML 与飞书共用同一套主结构**：不要出现飞书是正式研究、HTML 却退化成低质量审计页
- **核心功能体验是主体**：按视频出现顺序写，优先选与竞品差异最大、用户感知最强的 3–6 个功能模块
- **不要单独开一个巨大“证据拆解”章节**
- **不要把“时间戳 / 来源”做成到处重复的低价值字段**
- **图注要短**：像“径向菜单：把专业工具栏游戏化”，不要写“用于证明：……”
- **表格里的列名要像人写的调研，不像机器审计表**
- **尾部必须做一致性检查**：产品定位要和核心判断一致；亮点 / 不足要能回指到核心功能体验

默认章节顺序收敛为：

1. 一句话总结
2. 基本信息
3. 产品简介
4. 核心功能体验
5. 亮点与不足
6. 竞品对比
7. 结论与跟踪建议
8. 一致性检查
9. 附录：信息来源

对“核心功能体验”默认使用：

1. 小标题
2. 简述：2–4 句话讲清功能干什么、怎么用、关键交互
3. 体验截图：图片 + 来源视频时间戳
4. 截图描述：说明读者应该看见什么
5. 一句点评：亮点或问题在哪里

默认按 **视频中出现顺序** 写，但标题要尽量上升到用户能理解的能力模块，而不是逐帧动作。也就是说：

- 好标题：`世界优先的创作入口`、`多模态输入与快速试错`、`场景上下文驱动的迭代闭环`
- 差标题：`打开右键菜单`、`选择 variant`、`点击 capture`

默认建议：

- 前 5 分钟测试：`3–4` 个功能主题
- 完整正式报告：`4–6` 个功能主题

每个功能主题至少回答：

1. 视频里到底发生了什么
2. 用户如何使用、关键交互是什么
3. 亮点或问题在哪里

## 这次沉淀下来的核心写作技巧

把视频调研写厚，不靠“多写几条功能”，而靠下面这套转换：

### 技巧 1：从动作层，提升到能力主题层

不要直接把视频里看到的动作原样写成标题，例如：

- 打开菜单
- 选择 variant
- 点击 capture

而要先问：这些动作合起来，说明了产品的什么能力主题？

例如：

- `打开菜单 + Create Object + 教程引导` → **世界优先的创作入口**
- `参考图 + 草图 + variant` → **多模态输入与快速试错**
- `结果落地 + Capture 场景 + 下一轮生成` → **从资产生成走向场景迭代闭环**

### 技巧 2：核心功能体验是亮点 / 不足的证据来源

亮点与不足不能凭空写，必须能回指到“核心功能体验”里的模块、截图或时间戳。

正确顺序：

1. 先按视频顺序写 3–6 个核心功能体验
2. 再从这些体验里抽取亮点与不足
3. 最后在一致性检查里确认每条亮点 / 不足都有支撑

不要先写结论再硬找证据。

### 技巧 3：每个功能模块都用固定五步展开

每个核心功能体验默认按这五步展开：

1. **小标题**
2. **简述**：功能干什么、怎么用、关键交互
3. **体验截图**：图片和视频时间戳
4. **截图描述**：读者应该看见什么
5. **一句点评**：亮点或问题在哪里

如果少了“一句点评”，这一节通常只是记录，不是调研判断。

### 技巧 4：结论必须回到定位

“基本信息 > 产品定位”和“结论与跟踪建议 > 核心判断”必须一致。

默认顺序：

1. 先在基本信息里写清产品定位
2. 核心功能体验验证这个定位是否成立
3. 结论里的核心判断回到同一个定位

### 技巧 5：尾部做一致性检查

正式交付前必须增加“一致性检查”，至少回答：

1. 基本信息里的“产品定位”和结论里的“核心判断”是否一致？
2. 亮点 / 不足是否能在核心功能体验里找到对应支撑？

如果不一致，必须先修正文档，不要把矛盾留给读者。

“核心工作流 / 用户路径”默认作为“核心功能体验”里的一个功能模块处理；更推荐：

- 把它作为“核心功能体验”里的一个模块
- 工作流图先出现，再用 2～3 段解释闭环与关键摩擦点
- 只有当用户明确要求单独展开工作流时，再提升成独立大章

如果图片必须和表格更紧密：

- 优先把图放在对应功能行的图片列里，并配一句短图注
- 如果飞书表格排版受限，再把图放在该功能小节标题下
- **禁止**把所有图集中堆到后面，再单独解释

## 质量门槛

结束前确认：

- 截图支撑了对应结论，且带时间戳
- 官方事实优先来自官方来源
- 推断已显式标记
- `reference_doc_structure.md` 的模块都覆盖了
- `skills_consulted` 已记录实际使用 / 跳过 / unavailable
- 网页核验默认实际使用了 Tavily；如果没有，已记录用户明确批准的降级原因
- Tavily 的缺口已优先补 WebSearch，尤其是 pricing 页面中的月费、Bits、套餐额度、限制、日期
- Tavily 没抓到的关键事实（如价格、套餐额度、关键限制、日期）已继续用 WebSearch / `web.run` 或 `web-access` 补齐，或明确记录“补查后仍未找到”
- 图示按 `diagram_workflow.md` 放进对应章节，而不是堆到末尾
- `notes.html` 与飞书文档的主结构一致，没有出现“飞书是样例风格、HTML 还是证据流水账”的分裂
- 核心功能体验按“小标题 → 简述 → 体验截图 → 截图描述 → 一句点评”展开
- 基本信息里的“产品定位”和结论里的“核心判断”一致
- 亮点 / 不足都能在核心功能体验里找到对应支撑
- 文档尾部已增加“一致性检查”
- 飞书文档已实际创建并返回链接；如果没有，已记录用户明确批准的降级原因
- 最终 QA 对照 `references/checklist.md`
- 最终回复写明本地输出路径
